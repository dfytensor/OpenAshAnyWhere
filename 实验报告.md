# OpenASH v1 通过 CopyFirst/CopyAnywhere — 机制重设计实验报告

> 目标：让 OpenASH v1（cummax + gen_model 骨干）通过 CopyFirst 选择性记忆测试，
> **不使用任何既有方案**（慢尺度记忆 / 内容门控 / F-layer 线性递推 / 双路状态），从头设计新机制。

---

## 1. 任务定义

### 1.1 CopyFirst（位置 0 复制）
```
序列: [目标token] + [N个噪声token] + [END信号]
任务: 在最后一位输出位置 0 的目标 token
距离 N = 4, 64, 512, 2048, 8192, 16384, 32768, 65536
```
- 词表 32（0=END，1=IGNORE，2~31 随机 token）
- 损失只算最后一位；指标 = 各距离准确率
- **基线**：纯 OpenASH v1（cummax）best_loss=1.6，基本失败

### 1.2 CopyAnywhere（任意位置复制）
```
序列: [噪声... 目标在随机位置 P ...] + [END + 查询]
任务: 按查询输出位置 P 的 token（查询=距 END 的步数 或 绝对位置）
```

### 1.3 为什么 cummax 失败
cummax 状态单调递增、只保留"运行最大值"——**次序信息被摧毁**：
无法区分"第一个"和"任意一个"，且 token 专属信息被噪声的最大值覆盖。

---

## 2. 机制设计演化（14 轮实验）

### 阶段 A：Max-Age 追踪器（解决 CopyFirst）

核心思想：**给 cummax 加一个"年龄寄存器"**——每个通道记录"当前最大值是几步前写的"。
第一个 token 的幸存通道 = 全局最大 age → 可识别。

| # | 变体 | 关键改动 | best_loss | 结果 |
|:--|:--|:--|:--:|:--|
| 1 | 软门 + 乘性读出 | sigmoid(β(k−M)) 软 max | 1.47 | 86.7%@4，64 崩（软更新侵蚀首值） |
| 2 | 硬门 β=40 | 饱和门 | 3.30 | 全崩（梯度饱和，学不动） |
| 3 | STE 硬门 | 前向硬 max + 反向软梯度 | 3.30 | 全崩（读出太弱） |
| 4 | **结构化读出** | age 门控 σ(γ(A−θ)) + 关联查找 | **0.63** | **96.1%@4**（短距通了） |
| 5 | 全局迟滞 margin | 覆盖需超 0.3 | 1.26 | 33.6%@4（首写也被挡） |
| 6 | 随龄抗写 | margin=0.6·σ(γ_a(A−θ_a)) | 1.47 | 46%@4, **43%@64**, 12.5%@65K |
| 7 | 随龄抗写调参 | margin 减半提前 | 1.36 | 46.9%@4, 35.2%@64 |
| 8 | **首写倍增 amp=2** | t=0 键 ×3 写入 | **0.00003** | **✅ 100% @ 4→65K 全距** |
| 9 | 首写倍增 amp=3 | 同上 | **0.00002** | **✅ 100% 全距** |

**实验 8 胜出配置**：d=512，首写 ×(1+amp)=×3，硬 max + STE，age 门控 + 关联查找读出。

### 阶段 B：CopyAnywhere（任意位置）

| # | 变体 | 关键改动 | best_loss | 结果 |
|:--|:--|:--|:--:|:--|
| 10 | gap 查询 + σ=3 | 高斯按龄选通道 | 1.85 | near 28%，far 4.9%（邻居泄漏） |
| 11 | σ=0.5 | 精确按龄 | 2.86 | 全崩（梯度消失） |
| 12 | σ 退火 | 3.0→0.5 | 2.14 | near 6.6%（仍失败） |
| 13 | 强粘性 margin=2.0 | 写后 2 步不可覆盖 | 2.01 | 16K:84%, 32K:100% **（假象）** |
| 14 | 修复 age 重置 bug | 重置用更新前 M | 1.87 | 假象仍在 |
| 15 | 修复 eval 协议 | 目标真实任意位置 | 1.96 | near 17.6%, far 20.9%（诚实结果） |
| 16 | **QueryFirst** | 查询前置 + 查询门控放大写 | **0.00001** | **✅ 100% @ 4→65K 任意位置** |

### 关键调试发现

1. **age 重置 bug**（实验 13→14）：重置条件用了*更新后*的 M（`k > M_hard` 恒假）→ age 永不重置、寄存器冻结。
2. **eval 协议 bug**（实验 13→15）：`min_gap=g` 时 P∈{0,1}——长距离下目标总在开头，"任意位置"从未被测到；16K/32K 的 84-100% 是假阳性。
3. **容量墙**（实验 10-15 探针）：单轨 cummax 寄存器容量 ≈ d/17 ≈ 30 个 token——margin 大则新 token 写不进（寄存器 70 步后冻结），margin 小则旧值被盖（age==q 通道≈0）。**写入与保护共用同一个 margin，不可两全。**

---

## 3. 最终机制（零旧招，纯 cummax 家族）

### 机制一：Max-Age + 首写倍增（CopyFirst，END 查询）
```
t=0:  键 ×(1+amp) 写入 cummax        → 结构性不可覆盖
每步:  M = max(M, k)  (硬前向 + STE 软梯度)
      A = 通道 age (被覆盖清零, 否则+1)
读出:  σ(γ(A−θ)) 挑出高龄通道 → M×gate → 关联查找 → logits
```

### 机制二：QueryFirst（CopyAnywhere，开头查询）
```
序列: [QUERY 位置P] [噪声... 目标在P ...] [END]
t==P: 目标键 ×(1+amp) 放大写入 cummax  → 任意位置的"结构性保险"
读出:  σ(γ(M−θ)) 幅度门控挑出放大通道 → 关联查找 → logits
```

### 共同核心洞察
> **cummax 过选择性记忆的钥匙 = 给"被查询的写"一个结构性优先权。**
> 位置 0 用首写放大；任意位置用查询门控放大。
> 单调性从"记忆的诅咒"变成"记忆的保险箱"。

---

## 4. 最终成绩

| 任务 | 机制 | 参数 | 训练 | best_loss | 距离 4→65,536 |
|:--|:--|:--:|:--:|:--:|:--:|
| CopyFirst | Max-Age + 首写倍增 | 296K | 2000 步/140s | 0.00003 | **100% 全距** |
| CopyAnywhere | QueryFirst | 296K | 3000 步/97s | 0.00001 | **100% 全距** |
| （对照）纯 cummax | — | — | — | 1.6 | 失败 |

---

## 5. 代码清单

```
F:\OpenASH2605\copyfirst_redesign\
├── openash_age.py        实验 1-3 (软门/硬门/STE, 已被替代)
├── openash_age2.py       实验 4-9 最终版: Max-Age + 首写倍增 (CopyFirst ✅)
├── openash_age_any.py    实验 10-15: 任意位置探索 (容量墙分析)
└── openash_queryfirst.py 实验 16 最终版: QueryFirst (CopyAnywhere ✅)
```

## 6. 遗留问题与展望

### 6.1 单轨寄存器容量墙
- **END 查询 + 任意位置**在纯单调寄存器下不可解（写入竞争）：
  margin 大则新 token 写不进（寄存器 ~70 步后冻结），margin 小则旧值被盖。
- 多轨（位置 mod K 轮转）可放大容量但 65K 仍覆盖不完，且引入轨道歧义（gap 查询无法定位轨道）。
- **QueryFirst 的任务形式**：查询在开头——若任务强制 END 查询 + 任意位置，
  需要关联记忆（k⊗v 累积）类机制，属另一个机制家族。

### 6.2 从 CopyFirst 到大海捞针（NIAH）——路线图

CopyFirst 证明的是"**注册表层**的选择性记忆机制"；NIAH 是**内容寻址 + 真实语言 + 长上下文**的综合问题，
不能自动达成。差距与路线如下：

**三个关键缺口**

| 缺口 | 现状 | 需要的新机制 |
|:--|:--|:--|
| 查询形式 | 查询=位置（开头给出） | 查询=自然语言问题（结尾给出），针位置未知 |
| 检索方式 | 按地址（age/幅度）选通道 | **内容寻址读出**：问题向量 → 与寄存器内容匹配 |
| 语言能力 | 32 词表玩具 | 集成进真 OpenASH（SFT 3.60）用真实语料训练 |

**路线图（三步）**

1. **机制集成**：把 Max-Age / QueryFirst 的"放大写 + 门控读出"内建到 OpenASH
   DecoderLayer（在 cummax 状态旁加寄存器通道），用真实中文语料训练，
   验证对自然语言的基本记忆行为（短程事实复述）。
2. **内容寻址读出**（核心新机制）：寄存器每通道附带"键描述"（写作时的
   上下文摘要投影）；读出时用问题向量 q 与各通道键做点积匹配——
   `gate = σ(γ·(q·K_channel))`，选中内容相关的通道再读 M。
   这相当于"单调寄存器 + 内容门"的混合，仍属 cummax 家族扩展。
3. **长上下文容量**：多槽位寄存器（K 轨 × 槽内内容寻址）+ 段落级粒度
   （把"针"理解为句子而非 token，寄存器按句块写入）；
   验证 8K-128K 长度下"问题 → 定位 → 抽取"链路。

**可预期的里程碑**
- M1：OpenASH+寄存器在 4K 上下文复述单句事实（>90%）
- M2：内容寻址读出在 8K 上下文定位任意插入的针（>80%）
- M3：32K+ 上下文的针检索，与 HybridFRSM/Transformer 基线对比

### 6.3 多目标扩展
- QueryFirst 的"查询门控放大写"可推广到多目标（多个 QUERY 位置、多次放大写入，
  幅度分层 1+amp1 / 1+amp2 区分）——支持"记住多个位置"。

---

## 7. M1 里程碑：集成进真 OpenASH（768×12，真实语料）

### 7.1 集成设计（openash_reg.py）
```
真 OpenASH (768x12, 8头, vocab 23005, 84.9M fp16, full_sft_768_12.pth)
  + FactRegister (模型级, 输入侧):
    - MARK_OPEN 开启写入模式
    - MARK_CLOSE 时把前一个 token (答案值) 的键 ×(1+amp) 放大写入 M_reg (硬max+STE)
    - 每步注入: x_emb += inject(σ(γ(M-θ)) ⊙ M)  (幅度门控)
  + 词表 +3 (MARK_OPEN/MARK_CLOSE/QUESTION)
  decoder 层不变 (批量前向)
```

### 7.2 训练
- 数据：pretrain_cached 前 20 万条真实中文文本，随机位置插入标记事实
  （张三/李四… × 喜欢的数字/颜色/动物 × 一~十/红黄蓝…），尾部提问"X是什么？答案是Y"
- 微调：lr 1e-4，2000 步，batch 8，seq 256，bf16，答案位损失 ×5
- 收敛：**250 步 100%**（gap 32/128/512），loss 归零，共 653s

### 7.3 评估（eval_reg.py）

| gap | 16 | 64 | 256 | 512 | 1024 | 2048 | 4096 |
|:--|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| 准确率 | 100% | 100% | 100% | 100% | **100%** | 68.8% | 56.2% |

- 1024 = 训练长度(256)的 **4 倍外推**，仍 100%
- **寄存器本身距离无关**：gap 4096 探针 M_reg 与值键 cos=0.746（错键 0.019），分离度满分、预测正确
- **2048+ 掉点 = 基座 cummax 长序列失稳**（norm 单调增长，FRSM 谱系记载的老毛病），非寄存器问题

### 7.4 下一步
- **基座长序列稳定性**（cummax 状态范数失控）→ 解决后测 8K+ 的 M2
- M2：内容寻址读出（问题向量匹配寄存器通道键）

---


## 7.5 基座 cummax 长序列稳定性修复（openash_stable.py）

**病根**：cummax 状态逐位置单调累积，长序列下 gen_model 输入分布漂移——
基座 loss 1024 内稳定（2.4-2.7），2048 爆到 3.98，4096 爆到 6.23。

**修复**：逐位置状态范数 cap——每层每位置 cummax 状态超半径 R 按比例缩放
（可微 / 保序 / 训练推理一致），R=10 可学习。

**结果**（稳定版 + 1000 步适配微调）：

| 指标 | 修复前 | 修复后 |
|:--|:--:|:--:|
| 回忆 gap 2048 | 68.8% | **93.8%** |
| 回忆 gap 4096 | 56.2% | **87.5%** |
| 回忆 ≤1024 | 100% | 100% |
| loss 256→4096 增幅 | +3.8 | +2.5（更平） |

- 200 步重新收敛 100%@≤1024
- 注意：cap 后绝对 loss 仍偏高（8.8@256 vs 原 2.4），需更多微调恢复语言质量
  （1000 步只训 8000 条样本）；记忆任务的长距离短板已治住。


## 7.6 M2：多槽寄存器 + 内容寻址读出（openash_reg2.py）

**设计**：K=8 槽寄存器——每个 [MARK_OPEN 事实 MARK_CLOSE] 写入一槽
（值 token 键 ×(1+amp) 放大写 + 事实文本键均值作查询键）；
提问处 q=问题键均值，gate_k = σ(γ·(cos(q, K_k)−θ)) 内容寻址选槽，注入 Σ gate_k·M_k。

**训练**：每文档 2-4 个不同人名的事实，提问其中一个；2000 步（2188s）。
训练中 2/3 事实 gap 64 达 96%/92%。

**评估**（随机基线 ≈4%，值集 23 种）：

| 事实数 | gap 64 | gap 512 | gap 1024 | gap 2048 |
|:--|:--:|:--:|:--:|:--:|
| 2 | 78.1% | 81.2% | 68.8% | 43.8% |
| 3 | 68.8% | 62.5% | 68.8% | 62.5% |
| 4 | 75.0% | 53.1% | 53.1% | 37.5% |

**结论**：内容寻址方向验证成立（远高于随机）；长 gap/多事实损耗来自
槽位碰撞 + 基座长上下文余威。后续（M3）：去掉事实标记（无标记的针）、
提升槽分配策略（内容相似槽复用而非轮转）。

## 7.7 M3：无标记针（openash_reg3.py）— 学习式写入门

**设计**：去掉 [MARK] 标记。写入门 g_t = sigmoid(Wg(x_t)+b)（STE：前向硬/反向软），
门开则 span 累积（键均值=查询键，软最后 token=值，放大写），门落提交到下一槽；
提问处 cos 内容寻址读。门通过答案损失端到端学会"事实句才写、填充不写"。

**训练**：2500 步（1625s），250 步即 100%(gap64/512)。

**评估**（无标记针，随机基线 ≈4%）：

| gap | 32 | 64 | 256 | 512 | 1024 | 2048 | 4096 |
|:--|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| 准确率 | 100% | 96.9% | 100% | 100% | 96.9% | 87.5% | 37.5% |

**里程碑**：真实中文文本中的无标记事实句，≤1024 上下文近乎完美定位；
2048 87.5%；4096 衰减 = 基座长上下文稳定性余威（cap 已缓解未根治）。

**全程链路回顾**：
- 玩具级: CopyFirst/CopyAnywhere 100%@65K (Max-Age/QueryFirst)
- 真模型: M1 标记事实 100%@1024 → 稳定性cap → M2 多事实内容寻址 81%
  → M3 无标记针 100%@1024/87.5%@2048

### 7.8 M3 修复：写一次永不覆盖 + 门稀疏正则

**探针发现**：4096 崩溃的机制 = **轮转槽覆盖**——门会把填充 span 也提交进寄存器，
gap 4096 时 ~30 个填充 span 轮转一圈，把针所在的槽覆盖掉；
且 K 与问题键 cos 为负 → 内容寻址门≈0 → 模型实际靠基座 cummax 状态作答。

**修复**：
1. 提交只进**空槽**（写一次永不覆盖），全满则丢弃 span → 针必然存活
2. 门稀疏正则（0.02·mean(gate)）→ 减少填充提交

**消融**（寄存器 vs 纯基座）：512: 100/100；1024: 96/88；2048: 75/54 —— 寄存器确实承担任务。

**结果**：

| gap | 修复前 | 修复后 |
|:--|:--:|:--:|
| ≤512 | 100% | 100% |
| 1024 | 96.9% | 93.8% |
| 2048 | 87.5% | **93.8%** |
| 4096 | 37.5% | **87.5%** |
| 8192 | — | 50% |

4096 从 37.5% → 87.5%（病根精确命中）。8K+ 剩余衰减 = 读出管线经 12 层基座
的状态漂移（cap 缓解未根治），寄存器本身已可靠。

### 7.9 最终验证（n=16 大样本）

| gap | ≤512 | 1024 | 2048 | 4096 | 8192 | 12288 |
|:--|:--:|:--:|:--:|:--:|:--:|:--:|
| 准确率 | 100% | 93.8% | 93.8% | 93.8% | **93.8%** | **87.5%** |

- 层范数探针：2048 与 8192 各层输出范数几乎一致（14.6→50.7 vs 14.6→47.8）——cap 管住范数，无爆炸
- 早前 8192 的 50% 为 n=8 小样本方差（4/8），n=16 实测 93.8%
- **无标记针在 12K 真实中文上下文仍 87.5%**——机制链路完整收关

### 7.10 多事实 + 模板变体（M3 升级，问题 2+3 合流）

**发现链**：
1. M3 单针训练的多事实测试只有 58%（2 事实）——门/读出没学过"多事实场景"
2. 探针：gap 1024 时寄存器完全正常（针槽 cos 0.70 最高），但 **sigmoid 读出把 8 个槽全混入注入**（0.70→0.88 vs 0.60→0.82），模型答错
3. 监督门（BCE）+ 多事实数据后，门正确开启（K 非零），但 1024 仍断崖 → 问题在读出选择

**修复三件套**：
1. **softmax 竞争读出**（空槽掩 -inf，针槽胜出、其余抑制）
2. **训练加长 384**（门见 1.5× 填充负样本）
3. **负加权监督门 BCE**（填充 token 权重按正负比补偿，抑制假提交）

**最终成绩**（模板变体 + 多事实 + 语义匹配，随机基线 4%）：

| 事实数 | gap64 | gap512 | gap1024 | gap2048 |
|:--|:--:|:--:|:--:|:--:|
| 1 | 100% | 100% | 87.5% | 68.8% |
| 2 | 81.2% | 84.4% | **84.4%** | 75.0% |
| 3 | 71.9% | 87.5% | **87.5%** | 68.8% |

1024 断崖消失（12-31% → 84-88%）；多事实、模板变体（"是什么/是多少"、
"喜欢的是/最爱/偏好"）语义匹配同时成立。

## 8. 附：OpenASH 谱系背景

OpenASH 系列（F:\OpenASH2605）：
```
open_ash.py (5/25)      初代: cummax + gen_model 5分支乘性交互 (SFT loss=3.60 同规模最优)
open_ash_v2.py (5/31)   +sin/cos 相位门控 FFN (受 WDLM 启发)
frsmash 系列 (6/28-29)  cummax 骨干 + 慢尺度记忆 / F-layer / 双路融合 (长程记忆探索)
```
本报告的工作：**保留 OpenASH v1 骨架，不用上述任何旧招，重新设计选择性记忆机制**。

